# API Reference

The `coreason_meta_engineering` package exposes purely deterministic Abstract Syntax Tree (AST) injection capabilities, powered by `libcst`. 

Below is the automated reference derived from the pure functions and classes within the module.

::: coreason_meta_engineering
